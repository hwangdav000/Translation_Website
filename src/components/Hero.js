import React from 'react';

const Hero = () => (
    <header>
        <div class="hero-image">
            <div class="hero-text">Turtle Translations</div>

        </div>
    </header>
);

export default Hero;